/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['khulo.gov.ge'], //make it 'your-domain.com'
    },
}

module.exports = nextConfig
